<Window x:Class="NotesTaking.MVVM.View.CreateNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Note" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Note Title:"/>
        <TextBox x:Name="txtTitle" Grid.Row="1" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2" Text="Note Content:"/>
        <TextBox x:Name="txtContent" Grid.Row="3" Margin="0,5,0,10" TextWrapping="Wrap" AcceptsReturn="True" Height="100"/>

        <Button Grid.Row="4" Content="Save" Click="btnSave_Click" HorizontalAlignment="Right"/>
    </Grid>
</Window>
